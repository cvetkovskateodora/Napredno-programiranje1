1. Preconditions
- Open Github
- Repositories tab> New> Enter repository name - Napredno-programiranje1> create repository
2. Open the folder where the project is> right click> Git bash here
type: git config --global user.name "Teodora Cvetkovska"
git config --global user.email cvetkovska.teodora@uklo.edu.mk
git config --list
3. Once you are logged in create local project
type: pwd> enter
cd C:/Users/Teodora/Desktop/IKT58-Proekt51> enter
git init
git checkout -b Nov
git add .
git commit -m "Poraka"
git push
4. Go to github and see if the changes are there
5. Create pull request, and merge the changes